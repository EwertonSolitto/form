import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 200,
    backgroundColor: "#000",
    paddingBlock: 6,
    borderRadius: 24,
    alignItems: "center"  
  },
  text: {
    fontSize: 24,
    color: "#fff"
  }
});